{
  "name": "lab900-display-attributes",
  "version": "1.0.0",
  "description": "Toggle a floating overlay that shows the underlying attribute name above each lab900 form field.",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "zip": "npm run build && cd dist && zip -r -FS ../lab900-display-attributes.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
